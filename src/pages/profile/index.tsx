import { View } from '@tarojs/components';
import React, { memo } from 'react';
import './index.scss';

const Profile: React.FC = memo(() => {
  return <View className='test'>Profile</View>;
});

export default Profile;
