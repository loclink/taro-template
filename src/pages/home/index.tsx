import { View } from '@tarojs/components';
import React, { memo } from 'react';
import './index.scss';

const Home: React.FC = memo(() => {
  return <View className='test'>Home</View>;
});

export default Home;
